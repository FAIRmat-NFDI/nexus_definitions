category: base
doc: |
  Base class to describe specific analysis methods in electron microscopy.
  
  This base class represent a template how deeper NXem_method (with method
  renamed to the name of the method (e.g. adf, ebsd, eels, eds) specialized
  base classes can be defined with which to equip an (NXem) application
  definition with specific groups to document method-specific derived and
  analyzed quantities.
symbols:
type: group
NXem_method(NXobject):
  (NXprocess):
    doc: |
      Details how processing steps.
    sequence_index(N0):
  IMAGE_R_SET_EM(NXimage_r_set_em):
  IMAGE_C_SET_EM(NXimage_c_set_em):
  SPECTRUM_SET_EM(NXspectrum_set_em):
